// this represents shared container and other elements that might come from a design system (possibly a private npm package)
// may refactor later on

import styled from 'styled-components';

export const Container = styled.div`
  padding: 20px;
`;
